import { ProfileView } from "@/components/profile-view";

/** Your own page — the one the editor and admin write to. */
export default function MyProfilePage() {
  return <ProfileView showCustomize />;
}
